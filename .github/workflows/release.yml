# ────────────────────────────────────────────────────────────────────────────
#  Free Music Player — CI/CD Pipeline
#
#  Triggers:
#    - Push to 'main'         → build & lint (no publish)
#    - Tag push 'v*'          → build + publish to GitHub Releases
#
#  Secrets required for publishing:
#    GH_TOKEN          — GitHub PAT with `repo` scope (or use GITHUB_TOKEN)
#    (macOS signing)   — CSC_LINK + CSC_KEY_PASSWORD for code signing
#    (macOS notarize)  — APPLE_ID + APPLE_APP_SPECIFIC_PASSWORD + APPLE_TEAM_ID
# ────────────────────────────────────────────────────────────────────────────

name: Build & Release

on:
  push:
    branches: [main]
    tags: ['v*']
  pull_request:
    branches: [main]

permissions:
  contents: write

defaults:
  run:
    working-directory: free-music-player

jobs:
  # ─── Lint & Type-check ──────────────────────────────────────────────────
  check:
    name: Lint & Type-Check
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: free-music-player/package-lock.json

      - name: Install dependencies
        run: npm ci --ignore-scripts

      - name: Type-check renderer
        run: npx tsc --noEmit

      - name: Type-check electron
        run: npx tsc --noEmit -p electron/tsconfig.json

  # ─── Build (all platforms, no publish) ─────────────────────────────────
  build:
    name: Build (${{ matrix.os }})
    needs: check
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ${{ matrix.os }}
    timeout-minutes: 30

    strategy:
      matrix:
        os: [macos-latest, ubuntu-latest, windows-latest]

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: free-music-player/package-lock.json

      - name: Install dependencies
        run: npm ci --ignore-scripts

      - name: Compile Electron main process
        run: npx tsc -p electron/tsconfig.json

      - name: Build renderer (Vite)
        run: npx vite build

      - name: Package with electron-builder
        run: npx electron-builder --publish never
        # No GH_TOKEN here — prevents accidental publish on main branch builds

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: build-${{ matrix.os }}
          path: free-music-player/release/
          retention-days: 7

  # ─── Publish (sequential — release must exist before others upload) ───
  publish-linux:
    name: Publish Linux
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: free-music-player/package-lock.json
      - run: npm ci --ignore-scripts
      - run: npx tsc -p electron/tsconfig.json
      - run: npx vite build
      - run: npx electron-builder --publish always
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  publish-macos:
    name: Publish macOS
    needs: publish-linux
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: macos-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: free-music-player/package-lock.json
      - run: npm ci --ignore-scripts
      - run: npx tsc -p electron/tsconfig.json
      - run: npx vite build
      - run: npx electron-builder --publish always
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  publish-windows:
    name: Publish Windows
    needs: publish-linux
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: windows-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: free-music-player/package-lock.json
      - run: npm ci --ignore-scripts
      - run: npx tsc -p electron/tsconfig.json
      - run: npx vite build
      - run: npx electron-builder --publish always
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # NOTE: electron-builder --publish always creates the release + uploads
  # assets + generates update manifests (latest-*.yml). No additional
  # release step needed — they'd just conflict.
